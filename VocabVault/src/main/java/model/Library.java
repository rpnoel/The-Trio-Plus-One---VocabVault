package model;

import java.util.ArrayList;

/**
 * Represents a library containing a collection of books in a specified language.
 */
public class Library {
    /**
     * The list of books in the library.
     */
    private ArrayList<Book> bookList;

    /**
     * The language of the books in the library.
     */
    private Language bookLanguage;

    /**
     * Constructs a Library with a given list of books.
     *
     * @param books the initial list of books to add to the library
     */
    public Library(ArrayList<Book> books) {

    }

    /**
     * Adds a book to the library.
     *
     * @param book the book to add to the library
     */
    public void addBook(Book book) {

    }

    /**
     * Removes a book from the library.
     *
     * @param book the book to remove from the library
     */
    public void removeBook(Book book) {

    }

    /**
     * Retrieves a specific book from the library.
     *
     * @param book the book to retrieve from the library
     * @return the requested book as a Story object, if found
     */
    public Story getBook(Book book) {

    }
}

